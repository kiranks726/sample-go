{
  "name": "ctx-kitchensink-go",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "test": "sst test",
    "start": "sst start",
    "build": "sst build",
    "deploy": "sst deploy",
    "remove": "sst remove",
    "diff": "sst diff"
  },
  "eslintConfig": {
    "extends": [
      "serverless-stack"
    ]
  },
  "dependencies": {
    "@serverless-stack/cli": "^0.67.0",
    "@serverless-stack/resources": "0.67.0",
    "aws-cdk-lib": "2.7.0"
  },
  "devDependencies": {
    "jest": "^27.5.1",
    "xunit-viewer": "^7.1.5"
  }
}
