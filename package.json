{
  "name": "ctx-kitchensink-go",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "sst test",
    "start": "sst start",
    "build": "sst build",
    "deploy": "sst deploy",
    "remove": "sst remove"
  },
  "eslintConfig": {
    "extends": [
      "serverless-stack"
    ]
  },
  "dependencies": {
    "@serverless-stack/cli": "1.0.2",
    "@serverless-stack/resources": "1.0.2",
    "aws-cdk-lib": "2.7.0",
    "config": "^3.3.7"
  },
  "devDependencies": {
    "jest": "^27.5.1",
    "xunit-viewer": "^7.1.5"
  }
}
